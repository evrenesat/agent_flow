Prompt text is not stable, so do not add tests that assert the exact wording of prompts.
Check behavior, placeholders, or structure instead, because prompts are expected to change as workflow defaults evolve.
